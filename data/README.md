# Data

The Adult dataset is intentionally not committed to this repository.

Run:

```bash
python -m src.train
```

The training script downloads Adult (version 2) through scikit-learn/OpenML and preprocesses missing values and categorical variables inside pipelines.
